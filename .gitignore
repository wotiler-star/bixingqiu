# Dependencies
node_modules/
vendor/

# Build output
build/
dist/
*.zip

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
logs/

# Environment
.env
.env.local
.env.*.local

# Database
# ⚠️ 数据库备份严禁入库：本项目 dump 含 2206 个真实手机号、182 个密码哈希，
#    且本仓库为 public，一旦提交将永久留存于 git 历史无法彻底删除。
#    备份仅离线交付（本地 deploy/db/ 目录）。
*.sql
*.sql.gz
*.sqlite
deploy/db/

# PHP settings
*.ini
# 例外：Hostinger 部署必需的 PHP 运行时配置，必须纳入版本管理
!service/.user.ini
!service/admin/.user.ini

# IDE 元数据（Eclipse PDT，部署时不需要，且会暴露本地路径）
.buildpath
.project
.settings/

# 部署临时产物
release/
public_html/
*.tar.gz
# build-hostinger.sh 的自定义输出目录（RELEASE_DIR=dist-xxx）
dist-*/
# 删除被安全策略拦截时的暂存目录
.bxq-trash/
*.trash-*
